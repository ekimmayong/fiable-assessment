import React from 'react';

export const styles: {[key: string]: React.CSSProperties } = {
    inputForm: {
        display: 'flex',
        flexDirection: 'row',
        paddingLeft: '10px',
        alignItems: 'center',
    },
    formControl: {
        width: '100%'
    }
}